[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dummypy"
version = "0.2.0"
description = "A dummy Python analytics library for demonstration purposes."
authors = [{ name = "Mark Richardson", email = "mrichardson82@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.11"
keywords = ["dummypy", "analytics", "demo", "example"]
classifiers = [
    "Private :: Do Not Upload",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "numpy>=2.4.6",
    "attrs>=25.3",
    "pandas>=2.2",
]

[project.urls]
Homepage = "https://github.com/markrichardson/dummyrepo"
Repository = "https://github.com/markrichardson/dummyrepo"

# dependencies only needed by the notebooks
[dependency-groups]
notebook = [
    "loman>=0.6.0",
]

dev = [
    "marimo>=0.23.16",
    "plotly>=6.9.0",
]

test = [
    "pytest>=9.1.1",
    "hypothesis>=6.165.9",
]

lint = [
    "ruff>=0.16.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/dummypy"]

# bump-my-version auto-discovers only .bumpversion.toml, .bumpversion.cfg,
# setup.cfg and pyproject.toml. Without a table in one of them it does not fail —
# it falls back to `git describe` and reports the last reachable tag as the current
# version, so a release can be cut at a version already published.
#
# No `current_version` and no [[tool.bumpversion.files]] entry for the version:
# bump-my-version reads and rewrites PEP 621 [project].version natively, and a
# second copy of the number only drifts. Add a [[files]] entry per *additional*
# location, never for [project].version itself.
#
# commit/tag stay false because /rhiza:release makes the commit and the tag itself,
# so the changelog lands in the bump commit; a bare `bump-my-version bump` would
# otherwise add a second commit and a duplicate tag.
[tool.bumpversion]
allow_dirty = false
commit = false
tag = false

# The README footer restates the version for readers who never open pyproject.toml.
# It drifted silently before (0.1.4 -> 0.1.5 was a hand-written fix), so declare it
# here instead. The `**Version**: ` prefix anchors the match to that one line.
#
# The footer also carried a hand-maintained `**Last Updated**` date, which drifted the
# same way (it still read June 2026 after the August 0.2.0 release). It was removed
# rather than anchored here: the release date already lives in CHANGELOG.md and in the
# GitHub release, so a third copy is drift surface without new information. Don't
# re-add it — point readers at the changelog instead.
[[tool.bumpversion.files]]
filename = "README.md"
search = "**Version**: {current_version}"
replace = "**Version**: {new_version}"

# pandas ships no type stubs (PEP 561); the optional pandas-stubs package is
# third-party and lags the runtime. Silence mypy --strict's missing-import error
# for pandas without pulling in stubs that would churn under strict mode.
[[tool.mypy.overrides]]
module = ["pandas", "pandas.*"]
ignore_missing_imports = true

[tool.deptry.package_module_name_map]
marimo = "marimo"
pandas = "pandas"
loman = "loman"
attrs = "attrs"
numpy = "numpy"
plotly = "plotly"

